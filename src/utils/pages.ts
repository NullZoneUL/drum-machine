import Translation from '@assets/literals/literals';
import { DEFAULT_MAIN_PAGES } from './default_values';

export const numPages = {
  itemValues: Translation.pages,
  selectedIndex: DEFAULT_MAIN_PAGES - 1,
};
